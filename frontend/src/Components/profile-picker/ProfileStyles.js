import styled,{keyframes} from 'styled-components'

export  const ProfilePickerContainer=styled.div`
display:flex;
justify-content:center;
.avatar{
    display:flex;
    justify-content:center;
    align-items: center;
    img{
        transform:scale(1.3)
    }
}
`